
const UpdateStock = ({ updStock }) => {

    return (
        <div className="container d-flex justify-content-around mb-5">
            <button type="button" className="btn btn-outline-danger" onClick={updStock}>Actualiazar Stocks</button>
        </div>
    )

}

export default UpdateStock
